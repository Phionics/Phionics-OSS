# Phionics-OSS

Phionics is a minimal coherence-ranking membrane: generate multiple candidate outputs, then rank and select them using a scoring function.

This repository contains a tiny, runnable open-source starter.

## Quickstart

Run the minimal example:

```bash
python examples/01_minimal.py
